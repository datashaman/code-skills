"""
helpers/transport package.

Modules:
  normalize  — provider event → abstract event translation.
  poll       — polling pass implementation (transport.mode: polling).
  receiver   — local webhook receiver (gh_forward / self_hosted_webhook).
"""
