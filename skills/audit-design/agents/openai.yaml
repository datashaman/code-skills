interface:
  display_name: "Audit Design"
  short_description: "Design and WCAG accessibility audit for web UIs. Accepts a URL or local directory. Uses a live browser for URLs — handles SPAs and dynamically-rendered content. Scores contrast,..."
  default_prompt: "$audit-design"
policy:
  allow_implicit_invocation: true
